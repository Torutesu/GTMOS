# Taskloop for iOS

A small SwiftUI app, here so acceptance can prove the native path end to end:
its screens are rendered as HTML from the source below, served, filmed and cut
into a video without an Xcode toolchain anywhere in sight.

Nothing here is built. The files exist to be read — a screen is a declared
hierarchy of labelled controls, and that is all the render stage needs.
